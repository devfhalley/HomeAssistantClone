-- Migration template
-- Copy this file and rename it with a descriptive name and date (e.g., 2025_04_15_add_alerts_table.sql)

-- Description: [Add a brief description of what this migration does]

-- Add your migration SQL statements below
-- Examples:

-- Creating a new table
-- CREATE TABLE IF NOT EXISTS "new_table" (
--   "id" SERIAL PRIMARY KEY,
--   "name" TEXT NOT NULL,
--   "created_at" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
-- );

-- Adding a column to an existing table
-- ALTER TABLE "existing_table" ADD COLUMN IF NOT EXISTS "new_column" TEXT;

-- Modifying a column
-- ALTER TABLE "existing_table" ALTER COLUMN "column_name" TYPE INTEGER;

-- Creating an index
-- CREATE INDEX IF NOT EXISTS "index_name" ON "table_name" ("column_name");

-- Remember to test your migration locally before applying it to production